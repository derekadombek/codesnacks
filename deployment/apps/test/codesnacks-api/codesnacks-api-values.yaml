---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: weather-chart
  namespace: ${CODESNACKS_NAMESPACE}
spec:
  values:
    fullnameOverride: weather-chart-test
    releaseName: weather-chart-test
    image:
      tag: v1
    ingress:
      hosts:
        - host: 127.0.0.1
          paths:
            - path: /boise/us/
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: weather-chart
  namespace: ${CODESNACKS_NAMESPACE}
spec:
  interval: 1m
  url: ssh://git@github.com/derekadombek/codesnacks/deployment
  ref:
    branch: main
  secretRef:
    name: flux-system
  ignore: |
    # exclude all
    /*
    # include charts directory
    !/charts/   